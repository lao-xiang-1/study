---
sr-due: 2026-08-13
sr-interval: 11
sr-ease: 210
---
#review

假设信号 $f(t)$ 的傅里叶变换为 $F(j\omega)$，即 $f(t) \leftrightarrow F(j\omega)$，以下是连续时间傅里叶变换（CTFT）的基本性质：

### 对偶性质
$$
F(jt) \leftrightarrow 2\pi f(-\omega)
$$

### 1. 线性性质 (Linearity)
若 $f_1(t) \leftrightarrow F_1(j\omega)$， $f_2(t) \leftrightarrow F_2(j\omega)$，且 $a, b$ 为常数，则：
$$ a f_1(t) + b f_2(t) \leftrightarrow a F_1(j\omega) + b F_2(j\omega) $$

### 2. 时移性质 (Time Shifting)
信号在时域中平移 $t_0$，等效于在频域中乘以一个线性相位因子：
$$ f(t - t_0) \leftrightarrow F(j\omega) e^{-j\omega t_0} $$
- 时域的平移不会改变信号的幅度谱 $|F(j\omega)|$，只会改变其相位谱。

### 3. 频移性质 (Frequency Shifting)
信号在频域中平移 $\omega_0$，等效于在时域中乘以一个复指数信号（调制原理）：
$$ f(t) e^{j\omega_0 t} \leftrightarrow F(j(\omega - \omega_0)) $$

### 4. 尺度变换性质 (Time and Frequency Scaling)
若 $a$ 为非零实常数：
$$ f(at) \leftrightarrow \frac{1}{|a|} F\left(j\frac{\omega}{a}\right) $$
- **时域压缩** ($|a| > 1$) 对应 **频域扩展** 且幅度减小。
- **时域扩展** ($|a| < 1$) 对应 **频域压缩** 且幅度增大。

> [!NOTE]
> ### 1. 脉宽（时宽，Time Duration / Pulse Width）
> - **定义**：脉宽是指信号在**时域**中持续的时间长度，通常用 $\tau$ 表示。
> - **物理意义**：它反映了信号在时间轴上的集中程度。例如，一个持续时间很短的闪光或电脉冲，其脉宽就很小；而一个持续发声的纯音，其脉宽则很长（甚至是无限长）。
> 
> ### 2. 频宽（带宽，Bandwidth）
> - **定义**：频宽是指信号的频谱在**频域**中所占的频率范围，通常用 $B$（或 $\Delta f$、$\Delta \omega$）表示。
> - **物理意义**：它反映了信号包含的频率成分的丰富程度。实际信号的频谱通常无限延伸，但其绝大部分能量（或功率）往往集中在一定的频率范围内，这个包含主要能量的频率区间就称为有效频宽。
> 
> ### 3. 脉宽与频宽的反比关系（测不准原理）
>根据**尺度变换性质**（$f(at) \leftrightarrow \frac{1}{|a|} F\left(j\frac{\omega}{a}\right)$），我们可以得出时域和频域之间的一个根本性规律：**信号在时域的持续时间与其在频域的带宽成反比**。
> 
> $$ \text{脉宽} \times \text{频宽} \approx \text{常数} $$
> 
> - **时域压缩 $\Rightarrow$ 频域扩展**：如果让脉宽变窄（信号在时域变得非常剧烈、突变），那么为了合成这样陡峭的波形，就需要极高频率的信号成分参与，因此其频谱会展宽（频宽变大）。极端情况是冲激函数 $\delta(t)$，其脉宽趋于 $0$，而频谱恒为 $1$，频宽为无限大。
> - **时域扩展 $\Rightarrow$ 频域压缩**：如果让脉宽变长（信号在时域变得平缓），它所包含的高频成分就越少，频谱就会向低频集中，频宽变小。极端情况是直流信号（持续时间无限长），其频谱是一个位于 $\omega=0$ 处的冲激，频宽为 $0$。

### 6. 微分性质 (Differentiation)
**时域微分**：
$$ \frac{d^nf(t)}{dt^n} \leftrightarrow (j\omega)^n F(j\omega) $$
**频域微分**：
$$ -jt \cdot f(t) \leftrightarrow \frac{dF(j\omega)}{d\omega} $$
如：
$$te^-t \leftrightarrow \frac{1}{(1+j\omega)^2}$$

### 7. 时域积分性质 (Integration)
$$ \int_{-\infty}^{t} f(\tau) d\tau \leftrightarrow \frac{1}{j\omega} F(j\omega) + \pi F(0)\delta(\omega) $$
- 其中 $F(0) = \int_{-\infty}^{\infty} f(t) dt$ 是信号的直流分量。右侧第二项代表积分后可能出现的直流分量引起的冲激。

> [!tip] Tip
> 
> #### 1. 物理意义：直流分量的积累
> - $F(0)$ 是信号 $f(t)$ 在 $\omega = 0$ 处的频谱值。根据傅里叶变换公式 $F(j\omega) = \int_{-\infty}^{\infty} f(t) e^{-j\omega t} dt$，当 $\omega = 0$ 时，**$F(0) = \int_{-\infty}^{\infty} f(t) dt$**。
> - 这意味着 $F(0)$ 代表了原信号 $f(t)$ 在整个时间轴上的**总面积**（即直流分量/平均分量）。
> - 当你对 $f(\tau)$ 从 $-\infty$ 积分到 $t$ 时，如果原信号的总面积不为 0（即 $F(0) \neq 0$），积分后的信号随着时间 $t \to \infty$ 会稳定在一个非零的常数值。这个无限长存的常数（直流偏移）在频域中就会表现为 $\omega = 0$ 处的一个冲激函数 $\delta(\omega)$。
> 
> #### 2. 数学推导：通过阶跃函数卷积
> 时域积分操作可以等效为原信号 $f(t)$ 与单位阶跃函数 $u(t)$ 的**时域卷积**：
> $$ \int_{-\infty}^{t} f(\tau) d\tau = f(t) * u(t) = \int_{-\infty}^{+\infty} f(\tau) u(t-\tau) d\tau $$
> 
> 根据 **8. 卷积定理**（时域卷积对应频域相乘），积分后的频谱等于 $f(t)$ 的频谱乘上 $u(t)$ 的频谱：
> $$ \mathscr{F} \left[ \int_{-\infty}^{t} f(\tau) d\tau \right] = F(j\omega) \cdot U(j\omega) $$
> 
> 而单位阶跃函数 $u(t)$ 的傅里叶变换是一个基本公式：
> $$ u(t) \leftrightarrow \frac{1}{j\omega} + \pi\delta(\omega) $$
> - 第一项 $\frac{1}{j\omega}$ 代表阶跃函数发生跳变产生的高频到低频分量。
> - 第二项 $\pi\delta(\omega)$ 代表阶跃函数 $t>0$ 时永远为 1 所包含的直流常数分量。
> 
> 将 $U(j\omega)$ 代入相乘：
> $$ F(j\omega) \cdot \left( \frac{1}{j\omega} + \pi\delta(\omega) \right) = \frac{1}{j\omega} F(j\omega) + \pi F(j\omega) \delta(\omega) $$
> $$ =\frac{1}{j\omega} F(j\omega) + \pi F(0) \delta(\omega) $$
> 

### 8. 卷积定理 (Convolution Property)
**时域卷积定理**（系统分析的核心）：
$$ f_1(t) * f_2(t) \leftrightarrow F_1(j\omega) F_2(j\omega) $$
**频域卷积定理**（相乘定理）：
$$ f_1(t) \cdot f_2(t) \leftrightarrow \frac{1}{2\pi} F_1(j\omega) * F_2(j\omega) $$

### 9. 帕塞瓦尔定理 (Parseval's Relation)
时域计算的总能量等于频域计算的总能量，体现了能量守恒：
$$ \int_{-\infty}^{\infty} |f(t)|^2 dt = \frac{1}{2\pi} \int_{-\infty}^{\infty} |F(j\omega)|^2 d\omega $$

### 10. 共轭对称性质（Conjugate Symmetry）
**如果 $f(t)$ 是实信号，那么它的傅里叶变换 $F(j\omega)$ 具有共轭对称性。**

数学表达式为：
$$ F(-j\omega) = F^*(j\omega) $$
*(注：上标 $*$ 表示取复共轭)*

这个性质可以进一步推导出频域在**幅度**、**相位**以及**实虚部**上的具体对称规律：

#### 1. 幅度谱与相位谱的对称性
将 $F(j\omega)$ 写成极坐标形式 $F(j\omega) = |F(j\omega)| e^{j\varphi(\omega)}$，由共轭对称性可得：
- **幅度谱是偶函数**：$|F(-j\omega)| = |F(j\omega)|$ （即频谱图以原点左右对称）
- **相位谱是奇函数**：$\varphi(-\omega) = -\varphi(\omega)$ （即相位图关于原点奇对称）

#### 2. 实部与虚部的对称性
将 $F(j\omega)$ 写成直角坐标形式 $F(j\omega) = R(\omega) + jX(\omega)$：
- **实部是偶函数**：$R(-\omega) = R(\omega)$
- **虚部是奇函数**：$X(-\omega) = -X(\omega)$


### 11. 频域积分性质

假设 $f(t) \leftrightarrow F(j\omega)$，那么对其频谱 $F(j\omega)$ 从 $-\infty$ 到 $\omega$ 进行积分，其对应的时域表达式为：

$$ \pi f(0)\delta(t) + \frac{f(t)}{-jt} \leftrightarrow \int_{-\infty}^{\omega} F(j\eta) d\eta $$

为了更直观，通常我们将这个性质写成**时域除以 $t$** 的形式。如果信号在 $t=0$ 处的值为 0（即 $f(0) = 0$），那么右侧的冲激项 $\pi f(0)\delta(t)$ 就会消失，公式可以简化为：

$$ \frac{f(t)}{t} \leftrightarrow -j \int_{-\infty}^{\omega} F(j\eta) d\eta $$

*(注：在一些教材中，如果不限制积分下限，也常写为定积分形式： $\frac{f(t)}{t} \leftrightarrow j \int_{\omega}^{\infty} F(j\eta) d\eta$ )*
