
## 概念

PCA（Principal Component Analysis，主成分分析）是一种常用的无监督降维算法。核心思想是将高维数据投影到低维空间，同时最大化保留数据的方差（即信息量）。

它通过寻找数据中的“主成分”（新坐标轴），将原始多个相关特征转化为少数几个不相关的综合特征。主要用途包括：

· 降维：减少特征数量，降低计算复杂度，缓解“维度灾难”
· 可视化：将高维数据压缩到2D或3D进行展示
· 去噪：丢弃方差较小的成分，保留主要信号

例如，一个包含“身高（厘米）”“身高（米）”等冗余特征的100维数据集，用PCA可压缩到几十维，同时保留95%以上的原始信息。需要注意的是，PCA会改变特征的可解释性（新特征是原始特征的线性组合，意义更难直接解释）。

需要我进一步介绍PCA和另一种流行降维算法t-SNE的区别吗？

## 计算方法

PCA（主成分分析）的计算步骤可以概括为：中心化 → 协方差 → 特征分解 → 选择主成分 → 投影。下面以数据矩阵 X（n 个样本，d 个原始特征）为例，给出详细流程。

---

第一步：数据标准化（中心化 + 可选缩放）

· 中心化：对每个特征减去其均值，使每个特征均值为 0。
    原因：PCA 关注方差（二阶统计量），中心化保证第一主成分方向是方差最大方向。
· 标准化（可选）：若特征量纲差异大，通常再除以标准差（即标准化为均值为0、方差为1）。
    否则方差大的特征会主导主成分。

设中心化后的数据矩阵为 X_{\text{centered}}（形状 n \times d）。

---

第二步：计算协方差矩阵

协方差矩阵 C 为 d \times d 矩阵：

C = \frac{1}{n-1} X_{\text{centered}}^T X_{\text{centered}}

（也可用 \frac{1}{n}，只影响特征值的缩放，不影响特征向量方向）

协方差矩阵反映了原始特征之间的线性相关性。

---

第三步：求解特征值与特征向量

对协方差矩阵 C 进行特征分解：

C v_i = \lambda_i v_i

其中 \lambda_i 是特征值（标量，从小到大或从大到小排列），v_i 是对应的特征向量（长度为 d，代表主成分的方向）。

常用方法：若数据维度过高（d 很大），可以直接对 X_{\text{centered}} 做奇异值分解（SVD），避免显式计算协方差矩阵。

---

第四步：选择主成分

· 将特征值从大到小排序：\lambda_1 \ge \lambda_2 \ge \dots \ge \lambda_d，对应特征向量 v_1, v_2, \dots, v_d。
· 选择前 k 个特征向量（k \le d），构成投影矩阵 W = [v_1, v_2, \dots, v_k]（形状 d \times k）。
· 如何选择 k：
    通常用累计方差贡献率（Explained Variance Ratio）：
  \text{累计贡献率} = \frac{\sum_{i=1}^k \lambda_i}{\sum_{i=1}^d \lambda_i}
  一般取累计贡献率 ≥ 0.85 或 0.95 的最小的 k，或根据实际任务（如可视化取 k=2/3）。

---

第五步：数据投影（降维）

将原始中心化后的数据乘以投影矩阵，得到 n \times k 的低维表示：

Z = X_{\text{centered}} \, W

Z 的每一列是一个主成分（新特征），样本点在新坐标系下的坐标。

如果第一步做了标准化，则投影到主成分后得到的是标准化数据的主成分得分；若要重构回原空间（近似），可用 X_{\text{approx}} = Z W^T（再加回均值）。

---

一个小例子（直观理解）

假设二维数据点呈倾斜的椭圆分布：

1. 中心化后，原点位于数据重心。
2. 协方差矩阵的两个特征向量：长轴方向（第一主成分，方差最大）、短轴方向（第二主成分，正交）。
3. 若只保留第一主成分，所有点投影到长轴上（降为1维），信息损失最小。

---

注意事项

· PCA 对数据的尺度敏感：量纲不同的特征必须先标准化。
· PCA 是线性方法，无法处理非线性流形（此时可考虑核PCA、t-SNE、UMAP等）。
· 结果中的新特征不可解释，它们是原始特征的线性组合。

如果在代码中实现（如 Python scikit-learn），使用 sklearn.decomposition.PCA 即可一步完成，但理解上述步骤对调参和调试很有帮助。

## 例题


这张图片是一道主成分分析（PCA）的经典计算题。题目要求：**用PCA的方法将这组二维数据降到一维**。
给定的原始数据矩阵（每列代表一个样本点，共 5 个样本，每个样本有 2 个特征）为：

下面是完整的完整求解步骤：
### 第一步：零均值化（中心化）
首先需要检查数据的每一行（每一个特征）的均值是否为 0。
 * 第一行（特征 1）的均值：
   
 * 第二行（特征 2）的均值：
   
因为两个特征的均值已经都是 0，所以矩阵 X **不需要额外做减去均值的操作**，它已经是中心化数据了。
### 第二步：计算协方差矩阵
协方差矩阵 C 的计算公式为：


*(注：有时分母也使用 m-1，在学术计算题中通常使用总样本数 m=5)*
我们先计算 X X^T：

 * 左上角：(-1)^2 + (-1)^2 + 0^2 + 2^2 + 0^2 = 1 + 1 + 0 + 4 + 0 = 6
 * 右上角 / 左下角：(-1)(-2) + (-1)(0) + 0(0) + (2)(1) + (0)(1) = 2 + 0 + 0 + 2 + 0 = 4
 * 右下角：(-2)^2 + 0^2 + 0^2 + 1^2 + 1^2 = 4 + 0 + 0 + 1 + 1 = 6
所以：

代入分母 m = 5 得到协方差矩阵 C：

### 第三步：计算协方差矩阵的特征值
求解特征方程 \det(C - \lambda I) = 0：

解这个一元二次方程，可以因式分解为：

得到两个特征值：
 * \lambda_1 = 2 （最大特征值）
 * 因为我们要降到**一维**，所以选择最大的特征值 **\lambda_1 = 2**。
### 第四步：求最大特征值对应的特征向量
将 \lambda_1 = 2 代入 (C - \lambda I)v = 0：

可以得出：-0.8v_1 + 0.8v_2 = 0 \implies v_1 = v_2。
选择基础解系并将其**单位化（模长为1）**：

这就是我们要找的主成分投影方向（变换矩阵 P）。
### 第五步：将数据投影到一维空间
降维后的新数据 Y 相当于用变换矩阵 P = v^T 与原矩阵 X 相乘：

挨个计算每一列：
 * 第1个点：\frac{1}{\sqrt{2}}(-1) + \frac{1}{\sqrt{2}}(-2) = -\frac{3}{\sqrt{2}}
 * 第2个点：\frac{1}{\sqrt{2}}(-1) + \frac{1}{\sqrt{2}}(0) = -\frac{1}{\sqrt{2}}
 * 第3个点：\frac{1}{\sqrt{2}}(0) + \frac{1}{\sqrt{2}}(0) = 0
 * 第4个点：\frac{1}{\sqrt{2}}(2) + \frac{1}{\sqrt{2}}(1) = \frac{3}{\sqrt{2}}
 * 第5个点：\frac{1}{\sqrt{2}}(0) + \frac{1}{\sqrt{2}}(1) = \frac{1}{\sqrt{2}}
### 最终答案
降维后的一维数据矩阵为：

*(如果保留小数，结果约为：\begin{pmatrix} -2.12 & -0.71 & 0 & 2.12 & 0.71 \end{pmatrix})*

