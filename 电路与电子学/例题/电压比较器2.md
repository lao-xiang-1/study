---
sr-due: 2026-07-02
sr-interval: 5
sr-ease: 230
---

#review 

![704](assets/Pasted%20image%2020260624212057.png)

### （1）各运放组成的基本电路名称

*   **$A_1$ 组成的是：反相比例放大电路
*   **$A_2$ 组成的是：电压比较器**。
*   **$A_3$ 组成的是：电压跟随器

### （2）表达式与波形绘制

#### 1. $u_{o1}$ 与 $u_i$ 之间的表达式
对于理想运放 $A_1$ 构成的反相比例放大电路，根据虚短和虚断的原理，其闭环电压放大倍数为：
$$A_{v1} = -\frac{R_f}{R_1}$$
代入图中数值：
$$u_{o1} = -\frac{20\text{k}\Omega}{10\text{k}\Omega} \cdot u_i = -2u_i$$

已知 $u_i = 5\sin\omega t (\text{V})$，代入上式可得：
$$u_{o1} = -2 \times 5\sin\omega t = -10\sin\omega t (\text{V})$$
*(注：运放最大输出电压为 $\pm 15\text{V}$，此时峰值 $\pm 10\text{V}$ 未超出线性范围，运放正常工作。)*

#### 2. $u_{o2}$ 和 $u_o$ 的波形分析
*   **分析 $u_{o2}$**：
    电压比较器$A_2$输出：
$$
    u_{A_2} =
    \begin{cases}
    +U_{OM} = +15\text{V}, & u_{o1} < 0 \ (u_i > 0, \text{正半周}) \\
    -U_{OM} = -15\text{V}, & u_{o1} > 0 \ (u_i < 0, \text{负半周})
    \end{cases}
    $$
    经双向稳压管 $\text{VD}_Z$ 限幅后：
$$
    u_{o2} =
    \begin{cases}
    +U_Z = +6\text{V}, & u_{o1} < 0 \ (u_i > 0, \text{正半周}) \\
    -U_Z = -6\text{V}, & u_{o1} > 0 \ (u_i < 0, \text{负半周})
    \end{cases}
    $$
    因此 $u_{o2}$ 为与 $u_i$ **同相**的方波，幅值 $\pm 6\text{V}$。

*   **分析 $u_o$**：
    $A_3$ 是电压跟随器，起到阻抗变换和隔离的作用，其输出电压完全跟随输入电压。
    因此，**$u_o = u_{o2}$**。

#### 3. 波形图绘制

在画图时，需要将 $u_{o1}$、$u_{o2}$ 和 $u_o$ 的波形与输入信号 $u_i$ 对齐（横轴为相位 $\omega t$）。

*   **$u_{o1}$ 波形**：是一个反相的正弦波，峰值为 $-10\text{V}$ 和 $+10\text{V}$。
*   **$u_{o2}$ 波形**：是一个方波。在 $0 \sim \pi$ 期间为 $+6\text{V}$；在 $\pi \sim 2\pi$ 期间为 $-6\text{V}$。
*   **$u_o$ 波形**：与 $u_{o2}$ 完全相同。

