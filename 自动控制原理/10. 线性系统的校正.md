---
sr-due: 2026-08-23
sr-interval: 41
sr-ease: 270
---
#review

# 10. 线性系统的校正

## 一、 校正的基本概念
- **校正的目的**：当系统的基本结构和参数给定后，如果系统的性能指标不能满足设计要求，就需要引入附加的装置（校正元件/网络），以改变系统的结构或参数，使其满足预期的性能指标。

- **校正的方式**：
	- **串联校正**：校正元件与系统的前向通路串联。最常用，设计简单。
	- **反馈校正**（并联校正）：校正元件处于局部反馈回路中。能有效削弱非线性特性的影响，降低对元件参数变化的敏感度。
	- **前馈校正**：在闭环回路之外附加的开环控制通路（按扰动补偿或按输入补偿），用于提高系统的稳态精度，可实现“绝对不变性”。
	- **复合校正**：反馈控制（闭环）+ 前馈控制（开环）的结合。

## 二、 常用的串联校正网络

### 1. 超前校正 (Lead Compensation)
#### 传递函数：
$$G_c(s) = \frac{aTs+1}{Ts+1}$$
- 其中 $a > 1$

#### 频域特性：
令 $s = j\omega$，其频率特性的相角为：
$$\varphi(\omega) = \angle G_c(j\omega) = \arctan(aT\omega) - \arctan(T\omega)$$

由于 $a > 1$，总有 $\arctan(aT\omega) > \arctan(T\omega)$，因此 **$\varphi(\omega) > 0$**，即超前校正**始终提供正的相角**，这就是"超前"名称的由来。
- 提供正的相角超前，最大超前角出现在频率 $\omega_m = \frac{1}{T\sqrt{a}}$ 处。
- 具有高通滤波特性，使系统的截止频率 $\omega_c$ 增大，频带变宽。

#flashcards
#### 主要作用：
?
利用其相角超前特性来增大系统的相角裕度 $\gamma$，从而改善系统的瞬态响应（减小超调，缩短调节时间），提高系统的稳定性。
<!--SR:!2026-07-07,7,250-->

*超前校正的 bode图*
![](assets/10.%20线性系统的校正.png)

### 2. 滞后校正 (Lag Compensation)
#### 传递函数：
$$G_c(s) = \frac{Ts+1}{\beta Ts+1}$$
- 其中 $\beta > 1$

#### 频域特性：
令 $s = j\omega$，其频率特性的相角为：
$$\varphi(\omega) = \angle G_c(j\omega) = \arctan(T\omega) - \arctan(\beta T\omega)$$

由于 $\beta > 1$，总有 $\arctan(T\omega) < \arctan(\beta T\omega)$，因此 **$\varphi(\omega) < 0$**，即滞后校正**始终造成相角滞后**。

- 造成相角滞后，最大滞后角出现在频率 $\omega_m = \frac{1}{T\sqrt{\beta}}$ 处。
- 具有低通滤波特性，在中高频段产生负的幅值衰减（衰减量为 $20\lg \beta$），使系统的截止频率 $\omega_c$ 减小，频带变窄。

#flashcards
#### 主要作用：
?
利用其高频衰减特性，在不降低系统开环增益的前提下，使截止频率 $\omega_c$ 提前穿越 0dB 线，从而获得足够的相角裕度。主要用于提高系统的稳态精度（允许更大的开环增益），同时保持或改善稳定性，但会使系统的响应速度变慢。
<!--SR:!2026-07-05,5,240-->

*滞后校正的 bode图*
![](assets/10.%20线性系统的校正-1.png)

### 3. 滞后-超前校正 (Lead-Lag Compensation)
- **传递函数**：$G_c(s) = \frac{(T_1 s + 1)(T_2 s + 1)}{(aT_1 s + 1)(bT_2 s + 1)}$，通常取 $a > 1, b < 1$ 且 $ab=1$。
- **主要作用**：兼顾超前和滞后校正的优点，既能提高系统的稳态精度，又能改善系统的动态性能（加快响应速度并保持较高的稳定性）。低频段起滞后作用，高频段起超前作用。

## 三、 PID 控制器

在工程实际中，最广泛使用的串联校正装置是 PID 控制器（比例-积分-微分控制器）。
- **传递函数**：$G_c(s) = K_p \left( 1 + \frac{1}{T_i s} + T_d s \right)$
- **各部分作用**：
- **P (比例, Proportional)**：$K_p$，加快系统响应速度，但过大会导致系统不稳定。无法消除稳态误差。
- **I (积分, Integral)**：$\frac{K_p}{T_i s}$，提高系统的型别，消除稳态误差，提高稳态精度。但会引入相角滞后，降低系统稳定性。
- **D (微分, Derivative)**：$K_p T_d s$，反映误差的变化趋势，具有预测功能。相当于超前校正，能增加系统的阻尼程度，改善动态性能，但不影响稳态误差。
