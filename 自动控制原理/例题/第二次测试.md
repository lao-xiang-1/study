---
sr-due: 2026-06-28
sr-interval: 1
sr-ease: 230
---
#review

## 题1
![](assets/考试.png)

我们可以通过以下步骤来解答这两个问题。在题目中，开环传递函数中的参数写作小写 $s$ 和 $k$，而问题描述中写作大写 $S$ 和 $K$，在以下计算中我们将它们视为相同的变量。

---

### （1）通过劳斯判据当系统稳定时，求出 $K$ 和 $C$ 的取值范围

由题意，系统为单位负反馈系统，其开环传递函数为：
$$G(s)H(s) = \frac{C(s+k)}{s^2(s+1)}$$

闭环系统的特征方程为：
$$1 + G(s)H(s) = 0 \implies 1 + \frac{C(s+k)}{s^2(s+1)} = 0$$

展开整理得：
$$s^2(s+1) + C(s+k) = 0 \implies s^3 + s^2 + Cs + Ck = 0$$

我们可以得到特征多项式为：
$$D(s) = s^3 + s^2 + Cs + Ck$$

根据特征多项式的系数，列出**劳斯（Routh）表**：

| 行 | 第一列 | 第二列 |
| :--- | :--- | :--- |
| $s^3$ | $1$ | $C$ |
| $s^2$ | $1$ | $Ck$ |
| $s^1$ | $\frac{1 \cdot C - 1 \cdot Ck}{1} = C(1-k)$ | $0$ |
| $s^0$ | $Ck$ | |

系统稳定充要条件为劳斯表第一列所有元素均大于 0。
已知 $C > 0$，所以有：
1. $1 > 0$（满足）
2. $1 > 0$（满足）
3. $C(1-k) > 0 \implies 1-k > 0 \implies k < 1$
4. $Ck > 0 \implies k > 0$

综上所述，当系统稳定时，参数 $K$（即 $k$）和 $C$ 的取值范围为：
* **$C > 0$**
* **$0 < K < 1$**

---

### （2）若闭环极点全部位于垂线 $S = -1$ 和垂线 $S = 0$ 之间时，求出 $K$ 和 $C$ 的取值范围

设闭环极点（即特征方程的根）为 $s_i$（$i=1,2,3$）。题目要求所有极点全部位于垂线 $s = -1$ 与 $s = 0$ 之间，即：
$$-1 < \text{Re}(s_i) < 0 \quad (i=1,2,3)$$

这是一个双侧约束，我们可以将其拆分为两个条件来求解：

#### 条件一：极点实部小于 0（$\text{Re}(s_i) < 0$）
这即是系统稳定的条件，由第（1）问可知，其对应的范围为：
$$C > 0 \quad \text{且} \quad 0 < k < 1$$

#### 条件二：极点实部大于 $-1$（$\text{Re}(s_i) > -1$）
为了应用劳斯判据，我们可以通过变量代换将这一边界平移到虚轴。
令新变量为 $w = -(s + 1)$，即 $s = -w - 1$。
则条件 $\text{Re}(s_i) > -1$ 对应于新变量实部满足：
$$\text{Re}(w_i) < 0$$

我们将 $s = -w - 1$ 代入特征方程 $D(s) = 0$ 中：
$$D(-w-1) = (-w-1)^3 + (-w-1)^2 + C(-w-1) + Ck = 0$$

对各项进行展开：
* $(-w-1)^3 = -(w^3 + 3w^2 + 3w + 1)$
* $(-w-1)^2 = w^2 + 2w + 1$
* $C(-w-1) = -Cw - C$

代入并整理得：
$$-(w^3 + 3w^2 + 3w + 1) + (w^2 + 2w + 1) - Cw - C + Ck = 0$$
$$-w^3 - 2w^2 - (1+C)w + C(k-1) = 0$$

将等式两边同乘以 $-1$，得到关于 $w$ 的新特征方程 $Q(w) = 0$：
$$Q(w) = w^3 + 2w^2 + (1+C)w + C(1-k) = 0$$

要求所有根 $w_i$ 的实部均小于 $0$，我们可以对 $Q(w)$ 应用劳斯判据（或三阶多项式稳定性的赫尔维茨判据）：
对于三阶多项式 $w^3 + A_2 w^2 + A_1 w + A_0 = 0$，其稳定的充要条件为所有系数为正且 $A_2 A_1 > A_0$。
这里：
1. $A_2 = 2 > 0$（满足）
2. $A_1 = 1+C > 0 \implies C > -1$（由于已限制 $C > 0$，此条件自动满足）
3. $A_0 = C(1-k) > 0 \implies 1-k > 0 \implies k < 1$（由于已限制 $0 < k < 1$，此条件满足）
4. $A_2 A_1 > A_0 \implies 2(1+C) > C(1-k)$
   展开整理不等式：
   $$2 + 2C > C - Ck \implies C(1+k) + 2 > 0$$
   由于在条件一中我们已经有 $C > 0$ 且 $k > 0$，所以 $C(1+k) + 2$ 显然恒大于 0，该不等式自动成立。

因此，条件二在条件一的范围内是自然满足的。

---

#### 补充分析（从根之和的角度）：
我们可以从特征根结构进一步验证这个结果的物理合理性：
对于特征方程 $s^3 + s^2 + Cs + Ck = 0$，根据韦达定理，所有闭环极点的和为 $s^2$ 项系数的相反数：
$$s_1 + s_2 + s_3 = -1$$

* **若三个极点均为实根**（记为 $-\sigma_1, -\sigma_2, -\sigma_3$）：
  由于系统稳定，有 $\sigma_1, \sigma_2, \sigma_3 > 0$。
  它们的和为 $-(\sigma_1 + \sigma_2 + \sigma_3) = -1 \implies \sigma_1 + \sigma_2 + \sigma_3 = 1$。
  因为每个 $\sigma_i$ 都是正数，所以必有 $0 < \sigma_i < 1$，即极点全部位于 $-1$ 到 $0$ 之间。

* **若有一个实根和一对共轭复根**（记为 $s_1 = -\sigma$ 且 $s_{2,3} = -\alpha \pm j\beta$）：
  由于系统稳定，有 $\sigma > 0$ 且 $\alpha > 0$。
  它们的实部和为 $-\sigma - 2\alpha = -1 \implies \sigma + 2\alpha = 1$。
  由此可得：
  * 因为 $\alpha > 0$，所以 $\sigma < 1$，即实根满足 $-1 < s_1 < 0$。
  * 因为 $\sigma > 0$，所以 $2\alpha < 1 \implies \alpha < 0.5$，即复根实部满足 $-0.5 < \text{Re}(s_{2,3}) < 0$。
  因此，所有极点的实部依然全部位于 $-1$ 到 $0$ 之间。

综上所述，只要系统满足稳定条件，其闭环极点就一定会自发地全部位于垂线 $S = -1$ 和 $S = 0$ 之间。

### 最终取值范围：
* **$C > 0$**
* **$0 < K < 1$**

---

## 题2
![](assets/第二次测试.jpg)

## 题3
![](assets/第二次测试-1.jpg)