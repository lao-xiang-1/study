微机原理与接口技术
第3章 8051指令系统和汇编程序
设计
主讲人：王俊宏
Mob：18072745239
Office: 第二教研楼南楼514室

第3章 8051指令系统和汇编程序设计
3.2.2 算术运算类指令
算术运算指令是通过算术逻辑运算单元ALU进行数据运算与处
理的指令，主要完成加、减、乘、除四则运算，以及加1、减1、
BCD码运算和调整等。除加1、减1运算外，这类指令大多数要影
响PSW中的标志位。24条指令可分为6组。
 不带进位加法指令： 4条
 带进位加法指令： 4条
 带借位减法指令： 4条
 加1指令： 5条
 减1指令： 4条
 乘法、除法、十进制运算调整指令： 3条
３.2 指令系统 1

第3章 8051指令系统和汇编程序设计
3.2.3逻辑操作类指令
逻辑操作类指令包含逻辑与、逻辑或、逻辑异或、求反、左右
移位、清0等。该类指令不影响标志位，仅当其目的操作数为A时，
对奇偶标志位P有影响。24条指令可分为5组。
 逻辑与操作指令： 6条
 逻辑或操作指令： 6条
 逻辑异或指令： 6条
 累加器清0和取反指令： 2条
 循环移位指令： 4条
３.2 指令系统 2

第3章 8051指令系统和汇编程序设计
教学单元
| 1.  指令系统概述  | 7.  位操作指令 |             |
| ----------- | --------- | ----------- |
| 2. 寻址方式     | 8.        | 查表指令的应用     |
| 3.  数据传送类指令 | 9.        | 堆栈操作指令的应用   |
| 4. 算术运算类指令  | 10.       | 十进制调整指令的应用  |
| 5. 逻辑运算类指令  | 11.       | 逻辑指令与字节状态操作 |
| 6. 控制转移类指令  | 12.       | 转移指令的应用     |
3

第3章 8051指令系统和汇编程序设计
内容提要
主要介绍8051微控制器指令系统中改变程序执行
顺序的控制转移类指令，包括无条件转移指令、条件
转移指令、子程序调用和返回指令、空操作指令及功
能。
4

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
程序的顺序执行是由程序计数器（PC）自动增1来实现的，
要改变程序的执行顺序，控制程序的流向，必须通过控制转移类
指令实现，所控制的范围为程序存储器的64KB空间。8051MCU
的控制转移类指令，共17条，可分为4组。
 无条件转移指令： 4条
 条件转移指令： 8条
 子程序调用和返回指令： 4条
 空操作指令： 1条
３.2 指令系统
5

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
1.无条件转移指令(4条)
LJMP addr16 ；长跳转指令，跳转范围为64K； (PC) ← addr16
；3字节指令。（Long Jump)
AJMP addr11 ；绝对跳转指令，跳转范围为2K；
； (PC)=(PC)+2，(PC)0-10 ← addr11。
；2字节指令。(Absolute Jump}
SJMP rel ；短跳转指令，跳转范围－128～＋127
; (Short Jump）；(PC)=(PC)+2+rel
JMP @A+DPTR ；散转指令或间接跳转指令；
； （PC）← (A)+(DPTR);A的内容为8位无符号数。
； 单字节指令。(Jump Indirect)
 区别：跳转的范围不一样
原则上，用SJMP或AJMP的地方都可以用LJMP替代。AJMP已很少使用。
３.2 指令系统
6

第3章 8051指令系统和汇编程序设计
3.2.4  控制转移类指令
2.条件转移指令(8条)
(1)判零转移指令：
|                              | JZ  rel                             | ；A=0, 跳。（PC）（PC）＋2＋rel |                 |        |
| ---------------------------- | ----------------------------------- | ----------------------- | --------------- | ------ |
| （Jump if ACC equal Zero）     |                                     | ；A≠0,不跳,继续向下执行。即(PC)   |                 | (PC)＋2 |
|                              | JNZ  rel                            | ；A≠0, 跳。                | （PC）（PC）＋2＋rel |        |
| (Jump if ACC Not equal Zero) |                                     | ；A＝0,不跳,继续向下执行。即(PC)   |                 | (PC)＋2 |
| 例：                           | 设（A）＝01H，执行程序                       |                         |                 |        |
|                              | JZ       LABEL1   ；因为（A）≠  0，程序继续执行 |                         |                 |        |
|                              | DEC    A       ；（A）-1=00H           |                         |                 |        |
|                              | JZ       LABEL2   ；因为（A）=00H，程序转向标号 |                         |                 |        |
； LABEL2 的地址执行
LABEL1：------
LABEL2：------
３.2  指令系统
7

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
2.条件转移指令(8条)
(2)数值比较转移指令（均为3字节指令）
助记符： CJNE（Compare and Jump if Not Equal）
CJNE A, direct, rel
CJNE A， #data，rel
CJNE Rn， ＃data，rel
CJNE @Ri，＃data，rel
功能:
 对指定的两操作数进行比较，即(操作数1)－(操作数2)，比较结果仅影响标志位
C，2个操作数的值不变；
 比较不等，程序转移，目的地址=该指令的PC+3+rel，即 (PC) (PC) ＋3＋
rel ，且：
• 若(操作数1) ＞＝(操作数2)，C  0;
• 若(操作数1) ＜(操作数2)，C  1 。
 比较相等，程序继续顺序执行，即(PC)  (PC)＋3
３.2 指令系统
8

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
2.条件转移指令(8条)
(3)循环转移指令： DJNZ（Decrement and Jump if Not equal Zero）
DJNZ Rn，rel ;(Rn)  (Rn)-1;
;若(Rn)≠0,跳,（PC） (PC)＋2＋rel
;若(Rn)=0,不跳；即（PC） （PC）＋2
DJNZ direct，rel ; direct的内容为判断依据
功能:
• Rn或direct的内容减1，判别其内容是否为0。
• 若不为0，跳转到目标地址，继续执行循环程序；
• 若为0，则结束循环程序段，程序往下执行。
３.2 指令系统
9

第3章 8051指令系统和汇编程序设计
3.2.4  控制转移类指令
3.子程序调用和返回指令(4条)
(1)长调用指令：助记符LCALL(Long Subroutine Call)
| LCALL addr16  ;(PC) |        | (PC) ＋3 , 下条指令地址自动压栈保护； |               |
| ------------------- | ------- | ----------------------- | ------------- |
|                     | ;(SP)  | (SP) +1, ((SP))        | PCL，PC低8位入栈;  |
;(SP)(SP)+1 , ((SP)) 
PCH，PC高8位入栈；
|     | ; （PC） |  addr16（子程序首址） |     |
| --- | ------ | --------------- | --- |
该指令可调用存放在64KB ROM空间任何位置的子程序。
(2)绝对调用指令：助记符ACALL(Absolute Subroutine Call)
| ACALL addr11 | ;(PC)  | (PC) ＋2，PC自动压入堆栈保护; |     |
| ------------ | ------- | ------------------- | --- |
; PC(10~0) addr11 ,PC 的高5位不变,执行子程序。
被该指令调用的子程序入口地址必须与ACALL下一条指令在相同的2KB ROM空
间中。由于MCU的ROM容量不再是问题，所以这条指令很少使用。
３.2  指令系统
10

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
3.子程序调用和返回指令(4条)
(3)子程序返回指令：助记符RET(Return from Subroutine)
RET ；（PCH） （（SP）），（SP）  （SP）- 1
；（PCL）  （（SP）），（SP）  （SP）- 1
 功能：从堆栈顶部弹出子程序调用时，压入保护的断点地址到PC，即返回到主程
序。子程序的最后一条指令必须是RET指令。
(4)中断程序返回指令：助记符RETI(Return from Interrupt Subroutine)
除具有RET的功能外,还可以恢复中断逻辑。
• RET 和RETI决不能互换使用
• 中断子程序的最后一条指令，必须是RETI指令。
4.空操作指令：助记符NOP(No Operation)
NOP ；（PC） （PC）＋1
没有实际操作，只是花了一个机器周期时间。常用于软件延时。
３.2 指令系统
11

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
5.控制转移类指令举例
例：已知(SP)=60H，标号地址MA为0123H，SUB子程序的地址为5060H。分析执
行LCALL指令后，PC、SP以及堆栈顶部的数值。
（0123H） MA： LCALL SUB
（0126H） ……
……
ORG 5060H
（5060H）SUB: ……
……
RET
结果：（PC）＝5060H，（SP）＝62H，（61H）＝26H，（62H）＝01H。
３.2 指令系统
12

第3章 8051指令系统和汇编程序设计
3.2.4  控制转移类指令
5.控制转移类指令举例
例：下述指令执行后，问SP、A、B的内容分别是多少?
ORG   0100H
子程序中的2次PUSH，修改了保存
| 0100H      | MOV   SP, #40H |     |
| ---------- | -------------- | --- |
有断点地址的堆栈栈顶，使得REI指令
| 0103H      | MOV   A, #30H |     |
| ---------- | ------------- | --- |
自动恢复的断点是2次PUSH的内容，
| 0105H      | LCALL 0200H              |                    |
| ---------- | ------------------------ | ------------------ |
| 0108H      | ADD   A, #10H            | 所以子程序返回到了010AH的地址。 |
| 010AH      | MOV   B, A               |                    |
SP :          01, 0A, 01, 05
| 010CH L1:  | SJMP  L1 |     |
| ---------- | -------- | --- |
ORG    0200H
| 0200H     | MOV   DPTR, #010AH |     |
| --------- | ------------------ | --- |
| 0203H     | PUSH  DPL          |     |
| 0204H     | PUSH  DPH          |     |
结果：（SP）=42H，（A）=30H，（B）=30H
| 0205H  | RET |     |
| ------ | --- | --- |
３.2  指令系统
13

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
5.控制转移类指令举例
例：将片外RAM的30H单元开始的数据块传送到片内RAM 40H开始的位置，
直到出现零为止。(p69页)
MOV R0, #30H
MOV R1, #40H
LOOP: MOVX A, @R0
MOV @R1, A
INC R0
INC R1
JNZ LOOP
SJMP $
３.2 指令系统
14

第3章 8051指令系统和汇编程序设计
3.2.4 控制转移类指令
5.控制转移类指令举例
例：统计片内RAM中30H单元开始的20个数据中0的个数，放于R7中。
MOV R0, #30H
MOV R2, #20
MOV R7, #0
LOOP: MOV A, @R0
CJNE A, #0, NEXT ; 或 JNZ NEXT
INC R7
NEXT: INC R0
DJNZ R2, LOOP
SJMP $
３.2 指令系统
15

单选题
1分
执行 指令时 微控制器所完成
LCALL 4000H ,8051
的操作是
______
保护
PC
A
4000H→(PC)
B
保护现场
C
入栈
PC+3 ,4000H→(PC)
D
提交
16

单选题
1分
| 假定        |          | ，         |           | ，   |           |      | ，   |     |
| --------- | -------- | --------- | --------- | --- | --------- | ---- | --- | --- |
|           | (SP)=62H |           | (60H)=50H |     | (61H)=40H |      |     |     |
|           |          | ，         |           | ，执行 |           | 指令后， |     |     |
| (62H)=30H |          | (63H)=20H |           |     | RET       |      |     | PC  |
| 的值为       |          | 。         |           |     |           |      |     |     |
______
3020H
A
3040H
B
4030H
C
4050H
D
提交
17

单选题
1分
在 初值为 的情况下， ， 指令
R7 00H DJNZ R7 rel
将循环执行 次。
______
0
A
256
B
255
C
1
D
提交
18

第3章 8051指令系统和汇编程序设计
教学单元
| 1.  指令系统概述  | 7.  位操作指令 |             |
| ----------- | --------- | ----------- |
| 2. 寻址方式     | 8.        | 查表指令的应用     |
| 3.  数据传送类指令 | 9.        | 堆栈操作指令的应用   |
| 4. 算术运算类指令  | 10.       | 十进制调整指令的应用  |
| 5. 逻辑运算类指令  | 11.       | 逻辑指令与字节状态操作 |
| 6. 控制转移类指令  | 12.       | 转移指令的应用     |
19

第3章 8051指令系统和汇编程序设计
内容提要
主要介绍8051微控制器指令系统中的位操作类指
令及功能，包括位数据传送指令、位状态设置指令、
位逻辑运算指令和判位转移指令。
20

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
8051MCU具有一个功能齐全的位处理器，进位标志Cy为位累
加位，可以执行多种“位”操作。所有位操作均采用直接寻址方式，
寻址位空间为8051MCU的位寻址空间。17条位操作类指令可分为
4组。
 位数据传送指令： 2条
 位状态设置指令： 6条
 位逻辑运算指令： 4条
 位转移指令： 5条
３.2 指令系统 21

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
1.位数据传送指令(2条)
MOV C, bit ; C  （bit）
MOV bit，C ;（bit）  C
 功能：实现位累加器（C）和其它位地址之间的数据传递。
bit表示可位寻址的各位地址（8051MCU中，共有211位）
例：
MOV P1.0, C ; 将C中的状态送到P1.0引脚上去
MOV C, P1.0 ; 将P1.0的状态送给C
３.2 指令系统 22

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
2.位状态设置指令(6条)
(1)位清零指令：将C或指定位清零，助记符：CLR(Clear)
CLR C ; 使C=0
CLR bit ; 使指定位地址中的值等于0
例：CLR P1.0 ; 使P1.0变为0
(2)位置1指令：将C或指定位置1，助记符：SETB(Set Bit)
SETB C ; 使C=1
SETB bit ; 使指定位地址中的值等于1
例：SETB P1.7 ; 使P1.7变为1
３.2 指令系统 23

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
2.位状态设置指令(6条)
(3)位取反指令：将C或指定位取反，助记符：CPL(Complement)
CPL C ; 使C等于原来的相反的值，由1变为0，或由0变为1。
CPL bit ; 使指定的位等于原来相反的值，由0变为1或由1变为0。
3.位逻辑运算指令(4条)
(1)位与指令
ANL C, bit ; C与指定的位地址的值相与，结果送回C
ANL C,/bit ; 先将指定的位地址中的值取出后取反，再和C相与，
；结果送回C
注意：指定的位地址中的值本身并不发生变化
３.2 指令系统 24

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
3.位逻辑运算指令(4条)
(2)位或指令：
将指定位(bit)的内容或指定位内容取反后(原内容不变)与C的内容进行逻
辑或运算。结果仍存于C中。
ORL C,bit
ORL C,/bit
３.2 指令系统 25

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
4.位转移指令(5条)
位转移指令的功能分别是判断Cy或bit是1还是0，符合条件转移到目的地
址，否则继续顺序执行，共5条指令。
JC rel ;C=1, 跳到标号处，执行程序；C=0,不跳,顺序执行
; （Jump if the Carry flag is set）
JNC rel ;C=0, 跳;C=1,不跳，顺序执行
;（Jump if Not Carry）
JB bit，rel ;(bit)=1, 跳;(bit)=0,不跳（Jump if the Bit is set）
JNB bit，rel ;(bit)=0, 跳;(bit)=1,不跳
;（Jump if the Bit is Not set），
JBC bit，rel ;(bit)=1,跳，并使(bit)0;(bit)=0，不跳
; （Jump if the Bit is set and Clear the bit）
３.2 指令系统 26

第3章 8051指令系统和汇编程序设计
3.2.5  位操作类指令
5.位操作指令举例
例：设计程序实现P1.1和P1.2内容的互换。
| MOV | C, P1.1 | ;Cy  | P1.1 |     |
| --- | ------- | ----- | ---- | --- |

| MOV      00H, C |         | ;（00H） |       | Cy  |
| --------------- | ------- | ------ | ----- | --- |
| MOV             | C, P1.2 | ;Cy   | P1.2  |     |
| MOV             | P1.1, C | ;P1.1  |  Cy  |     |
| MOV             | C, 00H  | ;Cy   | （00H） |     |
| MOV             | P1.2, C | ;P1.2  |  Cy  |     |
例：设C=0，P0口的内容为00111010B。若执行以下指令后，分析C和P0的
内容。
CPL   P0.0
CPL    C
执行结果：C＝1，P0.0=1，即（P0）=00111011B
３.2  指令系统 27

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
5.位操作指令举例
例： 利用位逻辑运算指令编程实现图所示硬件逻辑电路的功能。
程序：
P1.0
与
MOV C,P1.0
P1.1
ANL C,P1.1
CPL C
或
ORL C,/P1.2
MOV F0,C
P1.5
MOV C,P1.3
P1.2
与
ORL C,P1.4
ANL C,F0
CPL C
P1.3
MOV P1.5,C
或
P1.4 SJMP $
３.2 指令系统 28

第3章 8051指令系统和汇编程序设计
3.2.5 位操作类指令
5.位操作指令举例
例：
从片外RAM的30H单元处开始有100个数据，统计当中正数、0和
负数的个数，分别放于R5、R6、R7中。
MOV R2,#100
NEXT1:CLR C
MOV R0,#30H
SUBB A,#80H
MOV R5,#0
JC NEXT2
MOV R6,#0
INC R7
MOV R7,#0
SJMP NEXT3
LOOP: MOVX A,@R0
NEXT2:INC R5
CJNE A,#0,NEXT1
NEXT3:INC R0
INC R6
DJNZ R2,LOOP
SJMP NEXT3
SJMP $
３.2 指令系统 29

单选题
1分
执行指令 后，结果被清 的是 。
CLR 30H 0 ______
的最低位
30H
A
的最高位
24H
B
单元
30H
C
单元的第 位
26H 0
D
提交
30

第3章 8051指令系统和汇编程序设计
教学单元
| 1.  指令系统概述  | 7.  位操作指令 |             |
| ----------- | --------- | ----------- |
| 2. 寻址方式     | 8.        | 查表指令的应用     |
| 3.  数据传送类指令 | 9.        | 堆栈操作指令的应用   |
| 4. 算术运算类指令  | 10.       | 十进制调整指令的应用  |
| 5. 逻辑运算类指令  | 11.       | 逻辑指令与字节状态操作 |
| 6. 控制转移类指令  | 12.       | 转移指令的应用     |
31

第3章 8051指令系统和汇编程序设计
内容提要
主要介绍8051微控制器指令系统中2条查表指令
（近程查表、远程查表）的应用，以及查表程序设计。
32

第3章 8051指令系统和汇编程序设计
3.3.1 查表指令
查表是指根据已知变量在表格中查找目标值的过程。在微控制器
应用中，经常会遇到查表操作，如LED数码管显示中查找BCD对应的
段码；微控制器系统中非线性补偿中查找修正系数以及数值换算等。
查表方法使得程序结构简单、执行速度快等优点。
8051MCU有近程查表和远程查表两条查表指令。
３.3 典型指令的应用 33

第3章 8051指令系统和汇编程序设计
3.3.1 查表指令
1.近程查表指令
MOVC A,@A+PC ;（PC）（PC）+1
; (A)  ((A)+(PC)) ； A中内容看作无符号数
 基址寄存器PC是下条指令首地址，即执行完查表指令后的PC，称为当前PC；
PC值不可改变。
 变址寄存器A是下条指令到常数表格中被访问字节的偏移量，范围是 0-255。
该指令只能查找本指令后256B范围内的数据表格，故称为近程查表。
３.3 典型指令的应用 34

第3章 8051指令系统和汇编程序设计
3.3.1 查表指令
2.远程查表指令
MOVC A,@A+DPTR ;(A)  ((A)+(DPTR))，A中内容看作无符号数
 基址寄存器DPTR是常数，指向数据表格的首地址；
 变址寄存器A为表格首址到被访问数据的地址偏移量；
 DPTR 、 A都可以改变，A的范围是0-255；DPTR的范围是0000H-
0FFFFH。
该指令可以查找存放在64KB范围内的数据表格，故称为远程查表指令。
３.3 典型指令的应用 35

第3章 8051指令系统和汇编程序设计
3.3.1  查表指令
3.查表指令举例
例：设R3中的值小于等于0FH。分别使用远程查表指令和近程查表指令，查出
R3的平方值，存回到R3中。
（1）远程查表方式
程序说明
|       | ORG   | 0100H        |     |
| ----- | ----- | ------------ | --- |
| SUB1: | MOV   | DPTR, #TABLE |     |
假设R3中的值为4
|     | MOV   | A, R3 |     |
| --- | ----- | ----- | --- |
MOVC     A, @A+DPTR
 远程查表： DPTR指向数据表头
|     | MOV        | R3, A |                  |
| --- | ---------- | ----- | ---------------- |
|     | SJMP       | $     | 的符号地址TABLE，待查数据（ |
|     | ORG 0150H  |       | R3）送入A，则查表指令从    |
TABLE:  DB 00, 01,04,09,16,25,36,49,
TABLE+4这个单元取出的数据就
64,51H,64H,121,144,0A9H,
是4的平方值16。
0C4H,0E1H
END
３.3  典型指令的应用 36

第3章 8051指令系统和汇编程序设计
3.3.1  查表指令
3.查表指令举例
程序说明
| （2）近程查表方式 |       |     |     | 假设R3中的值为4 |     |
| --------- | ----- | --- | --- | --------- | --- |
| ORG       | 0100H |     |     |           |     |

近程查表：当前PC指向下一条指令首址
| SUB1: | MOV   | A, R3 |     |     |     |
| ----- | ----- | ----- | --- | --- | --- |
3
| 0101H | ADD | A,#REL     ;REL=？， |     |     |     |
| ----- | --- | ------------------ | --- | --- | --- |
（PC=0104H），待查数据（R3）送入A
| 0103H | MOVC     A, @A+PC； |     |     |     |     |
| ----- | ------------------ | --- | --- | --- | --- |
（（A）=4），此时若不对A进行修正，
| 0104H | MOV  | R3, A；1个字节 |     |     |     |
| ----- | ---- | ---------- | --- | --- | --- |
则（A+PC）=0108H，从0108H单元取
| 0105H | SJMP | $；  | 2个字节 |     |     |
| ----- | ---- | --- | ---- | --- | --- |
数得到01H，结果错误。对于近程查表指
TABLE:  DB 00, 01,04,09,16,25,36,49,
(0107H)     64,51H,64H,121,144,0A9H,
令，通常需要对A进行修正，修正方法是
0C4H,0E1H
加上一个修正值REL，REL是当前PC到数
END
据表首址的间隔字节数。本例中，间隔的
REL=3：修正值是MOVC指令后一条指令
首址，与表头地址的间隔。
2条指令共3字节，所以REL=3。修正后
（A）=7，则将从010BH单元取数，得
| ３.3  典型指令的应用 |     |     |     | 到16，结果正确。 | 37  |
| ------------ | --- | --- | --- | --------- | --- |

THE END
THANK YOU
38