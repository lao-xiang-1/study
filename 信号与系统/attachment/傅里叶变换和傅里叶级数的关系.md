---
sr-due: 2026-09-28
sr-interval: 3
sr-ease: 250
---
#math 

>简单来说，傅里叶级数就是傅里叶变换的特殊取值

## 傅里叶级数
设 $f(t)$ 为周期函数，其周期为 $T$ ，基频率 $\omega = \frac{2\pi}{T}$，考虑傅里叶级数的指数形式：
$$
f(t) = \sum_{n=-\infty}^{\infty} c_n e^{jn\omega_n t}
$$
- $\omega_n = n\omega$，$(n = 0, \pm1, \pm2, \dots)$

**其系数计算公式：**
$$
c_n = \frac{1}{T} \int_{t_0}^{t_0+T} f(t) e^{-jn\omega_n t} \, dt 
$$

## 傅里叶变换
而我们写出傅里叶变换的公式：
$$F(\omega) = \int_{-\infty}^{\infty} f(t) e^{-i \omega t} dt$$
若取 $f^{'}(t)$ 为 $f(t)$ 的其中一个周期（其余部分全置0），那么它的傅里叶变换为：
$$F^{'}(\omega) = \int_{t_0}^{t_0+T} f^{'}(t) e^{-i \omega t} dt$$
只需要乘以 $\frac{1}{T}$，在取 $\omega$ 为离散值，那便与上面提到的系数计算公式一样。

#### 直觉建立
也就是说：对于一个函数，傅里叶变换是求出它连续的频率值；而傅里叶级数则是求离散值

将级数 $f(t) = \sum_{n=-\infty}^{\infty} c_n e^{jn\omega_0 t}$ 做傅里叶变换，得到的是一系列冲激函数：
$$
F(j\omega) = \sum_{n=-\infty}^{\infty} c_n \,\mathcal{F}\{e^{jn\omega_0 t}\} = 2\pi \sum_{n=-\infty}^{\infty} c_n \,\delta(\omega - n\omega_0)
$$
也就是说，当一个函数周期化时，其每一个频率都被无限放大到冲激函数

> **对偶关系**：时域周期化 $\Longleftrightarrow$ 频域离散化（冲激串）；时域离散化（采样）$\Longleftrightarrow$ 频域周期化。这正是傅里叶级数与傅里叶变换能够互相转化的根源。

## 从傅里叶级数到傅里叶积分
>把周期看作无穷大，傅里叶级数便可以转成积分形式，即是从周期函数转到非周期函数

对于任意一个非周期函数 $f(t)$ ，都可以截取 $[-\frac{T}{2},\frac{T}{2})$ 区间，并按周期  $T$ 延拓到整个数轴上。记延拓后的周期函数为 $f_T(t)$ ，该函数可以展开为傅里叶级数：
$$
f_T(t) = \sum_{n=-\infty}^{\infty} \frac{1}{T} \left[\int_{-\frac{T}{2}}^{\frac{T}{2}} f(t) e^{-jn\omega_n t}\, dt \; \right] e^{jn\omega_n t}
$$
- 这里直接将 $c_n$ 展开

显然，周期 $T$ 取得越大，$f_T(t)$ 就越接近 $f(t)$ 。当 $T \to \infty$ 时，$f(t) = f_T(t)$，即
$$
f(t) = \lim_{T \to \infty}\sum_{n=-\infty}^{\infty} \frac{1}{T} \left[\int_{-\frac{T}{2}}^{\frac{T}{2}} f(t) e^{-jn\omega_n t}\, dt \; \right] e^{jn\omega_n t}
$$
当 $T \to \infty$ 时，间隔 $\nabla \omega_0 = \frac{2\pi}{T} \to 0$，上式可变化为：
$$
f(t) = \lim_{\nabla \omega_0 \to 0}\sum_{n=-\infty}^{\infty} \frac{1}{2\pi} \left[\int_{-\infty}^{\infty} f(t) e^{-jn\omega_n t}\, dt \; \right] e^{jn\omega_n t } \, \nabla \omega_0
$$
也就相当于关于 $\omega$ 积分：
$$
f(t) = \frac{1}{2\pi} \int_{-\infty}^{\infty}  \left[\int_{-\frac{T}{2}}^{\frac{T}{2}} f(t) e^{-jn\omega t}\, dt \; \right] e^{jn\omega t } \, d \omega
$$

